<?php
      // Paramètres serveur
      // Adresse IP, Port et construction de l'url

	  $server_host = "127.0.0.1";
	  $server_port = "8080";

	  $server_url = "http://".$server_host.":".$server_port;

?>
